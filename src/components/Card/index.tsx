"use client";
import React, { useRef, useEffect } from "react";
import "./style.css";

interface Props {
  // clienY: number;
}

const Card: React.FC<Props> = () => {

  return (
    <div className="card">
        <h2>1 號</h2>
    </div>
  );
};

export default Card;
